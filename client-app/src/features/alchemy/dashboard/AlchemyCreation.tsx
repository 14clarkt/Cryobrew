import { observer } from 'mobx-react-lite';

export default observer(function AlchemyCreation() {
    return (
        <> 
            <div style={{color:'white'}}>creation. Shows what a finished product would look like, its base cost, DC, and time</div>
        </>
    )
})