import { observer } from "mobx-react-lite"

export default observer(function CrelicsDashboard() {
    return (
        <div style={{color:"white"}}>Crelics details and upgrades. To be implemented</div>
    )
})