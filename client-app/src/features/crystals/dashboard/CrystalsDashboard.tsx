import { observer } from "mobx-react-lite"

export default observer(function CrystalsDashboard() {
    return (
        <div style={{color:"white"}}>Crystal item details and upgrades. To be implemented</div>
    )
})